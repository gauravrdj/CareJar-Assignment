<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to CareJar</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap">
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Montserrat', sans-serif;
            background-color: #f2f9ff; /* Light blue background */
            color: #333;
            overflow-x: hidden; /* Prevent horizontal scrollbar */
        }

        header {
            background-color: #3498db;
            color: #ffffff;
            text-align: center;
            padding: 20px;
            font-size: 24px;
            letter-spacing: 2px;
        }

        section {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 400px;
        }

        .animation-container {
            text-align: center;
            max-width: 600px;
        }

        #logo {
            max-width: 100%;
            height: auto;
            margin-bottom: 20px;
        }

        .animated-text {
            font-size: 24px;
            font-weight: bold;
            opacity: 0;
            animation: fadeInUp 1.5s ease-in-out 0.5s forwards, colorChange 3s infinite;
            color: #3498db;
        }

        @keyframes fadeInUp {
            0% {
                opacity: 0;
                transform: translateY(20px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes colorChange {
            0% {
                color: #3498db;
            }
            50% {
                color: #219d54;
            }
            100% {
                color: #3498db;
            }
        }

        .explore-button {
            display: inline-block;
            padding: 10px 20px;
            font-size: 18px;
            font-weight: bold;
            text-decoration: none;
            color: #ffffff;
            background-color: #27ae60;
            border-radius: 5px;
            opacity: 0;
            animation: fadeIn 1s ease-in-out 1.5s forwards;
            transition: background-color 0.3s ease-in-out;
        }

        .explore-button:hover {
            background-color: #219d54;
        }

        @keyframes fadeIn {
            0% {
                opacity: 0;
                transform: translateY(20px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>
    <header>
        <h1>Welcome to CareJar</h1>
    </header>

    <section>
        <div class="animation-container">
            <img id="logo" src="images/logo.png" alt="CareJar Logo">
            <div class="animated-text">CARE JAR: Pick your health card
                And, we’ll add quality CARE to it.</div>
            <br>
            <a href="/home" class="explore-button">Start Consulting!</a>
        </div>
    </section>
    <script>
        setTimeout(function(){
           window.location.href='/home';
        },5000);
    </script>
</body>
</html>
